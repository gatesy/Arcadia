namespace Arcadia

module Evaluate =
    
    /// Evaluates the position each player has on the board.
    /// 1.0 -> White wins
    /// -1.0 -> Black wins
    /// 0.0 -> no one has won (yet!)
    let position board : float =
        0.0

